# Block 1 (Vandanaa) — schema load + embedding pipeline.
psycopg2-binary==2.9.9
pgvector==0.2.5
sentence-transformers==2.7.0
python-dotenv==1.0.1
tqdm==4.66.4

# Block 2 (Prakruti) — FastAPI backend + hybrid retrieval endpoint.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
